<div dir="rtl">

# دليل الإعداد والنشر — رَصيد

> هذا الملف موجّه لمن سيرتّب المشروع على GitHub (أنت أو مساعد ذكي مثل ChatGPT).
> اتبع الخطوات بالترتيب، والمشروع جاهز للعمل فورًا — كل الأكواد في ملف واحد.

---

## ١) الملفات المطلوبة (ضعها كلها في جذر المستودع)

| الملف | الوصف | إلزامي؟ |
|------|-------|--------|
| `index.html` | التطبيق بالكامل (HTML + CSS + JavaScript مدمجة في ملف واحد). لا يعتمد على أي مكتبة خارجية ولا خادم. | **نعم** |
| `README.md` | وصف المشروع وتعليمات التشغيل والنشر. | مستحسن |
| `LICENSE` | رخصة MIT. | اختياري |
| `.gitignore` | تجاهل ملفات النظام والمحررات. | اختياري |

**شرط وحيد للعمل:** أن يكون اسم ملف التطبيق `index.html` وأن يكون في **جذر** المستودع (ليس داخل مجلد فرعي).

---

## ٢) إنشاء المستودع ورفع الملفات

### الطريقة (أ) — عبر سطر الأوامر
```bash
# داخل مجلد يحتوي الملفات الأربعة
git init
git add .
git commit -m "رَصيد: النسخة الأولى"
git branch -M main
git remote add origin https://github.com/<USERNAME>/<REPO>.git
git push -u origin main
```

### الطريقة (ب) — عبر واجهة GitHub
1. أنشئ مستودعًا جديدًا (Public).
2. اضغط **Add file → Upload files**.
3. اسحب الملفات الأربعة وارفعها، ثم **Commit**.

---

## ٣) تفعيل النشر (GitHub Pages)

1. افتح المستودع → **Settings** → **Pages**.
2. تحت **Build and deployment**:
   - Source: **Deploy from a branch**
   - Branch: **main** والمجلد **/ (root)**
3. احفظ، وانتظر دقيقة إلى دقيقتين.
4. سيظهر رابط الموقع:
   `https://<USERNAME>.github.io/<REPO>/`

---

## ٤) التحقق من نجاح التشغيل

افتح الرابط، ويجب أن تظهر لوحة «رَصيد» كاملة. جرّب:
- تعديل الراتب، وتعبئة خانة يومية في «المصروف الشهري».
- أعد تحميل الصفحة (Refresh) → يجب أن تبقى البيانات محفوظة (تُحفظ تلقائيًا في `localStorage`).

---

## ملاحظات تقنية

- **لا يوجد build step**: المشروع ملف ثابت (Static)، يعمل مباشرةً.
- **لا توجد متغيّرات بيئة أو مفاتيح**.
- **التخزين** محلي في متصفّح كل مستخدم (`localStorage`)، فلكل جهاز بياناته الخاصة، ولا يوجد خادم أو قاعدة بيانات.
- يعمل أيضًا على **Vercel / Netlify** كموقع ثابت دون أي إعداد إضافي.

</div>
